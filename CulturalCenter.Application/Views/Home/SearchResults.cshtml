@model IList<Domain.Models.Book>

<h2>Search Results</h2>

@if (!Model.Any())
{
    <p>No results found.</p>
}
else
{
    <ul>
        @foreach (var book in Model)
        {
            <li>@book.Name by @book.Author.Name (Category: @book.Type)</li>
        }
    </ul>
}

